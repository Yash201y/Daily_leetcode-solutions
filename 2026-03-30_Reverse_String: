class Solution {
public:
    void reverseString(vector<char>& s) {
      int st=0, e=(s.size()-1);
        while(st<e){
          char t=s[st];
          s[st]=s[e];
          s[e]=t;
          st++;
          e--;
        }
    }
};
